Captured from `docker run --rm --name mr-secrets-env-verify4 -e MACHINE_RUN_TEST_SECRET=sup3rEnvSecret ... node:22-slim node check.mjs`,
running effect@4.0.0-rc.108's real `Config.redacted` against its real, unmocked
default `ConfigProvider` (no `ConfigProvider.fromEnvRecord`/`fromEnv({env})`
override) inside a container started with `MACHINE_RUN_TEST_SECRET` set and
`MACHINE_RUN_TEST_SECRET_DOES_NOT_EXIST` never set. See
docs/notes/secrets-env-notes.md for the full script and session.

$ node check.mjs
SET_VAR: {"_id":"Exit","_tag":"Success","value":"<redacted>"}
UNSET_VAR: {"_id":"Exit","_tag":"Failure","cause":{"_id":"Cause","failures":[{"_tag":"Fail","error":{"_tag":"ConfigError","name":"ConfigError", ... (SchemaError/InvalidType wrapping the missing env var, real captured shape, trimmed here) ...}}]}}

A second run against the same container, unwrapping the `Redacted` value to
confirm it is not merely "Success" but the real literal string:

$ node check2.mjs
UNWRAPPED: sup3rEnvSecret
