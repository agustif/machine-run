Captured from `doppler secrets get SOME_SECRET --plain --project someproj --config someconfig`
(Doppler CLI v3.76.4, installed fresh via the official apt repo, no
`DOPPLER_TOKEN` set and never logged in) inside `docker run --rm --name
mr-secrets-doppler-verify3 debian:stable`. Exit code 1. See
docs/notes/secrets-doppler-notes.md for the full session.

Doppler Error: you must provide a token
