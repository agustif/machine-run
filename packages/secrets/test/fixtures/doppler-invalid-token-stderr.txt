Captured from `doppler secrets get SOME_SECRET --plain --project someproj --config someconfig`
with `DOPPLER_TOKEN=dp.st.dev.thisisnotarealtoken1234567890` (a syntactically
plausible but fake service-account token, never issued by any real Doppler
account) inside `docker run --rm --name mr-secrets-doppler-verify4
debian:stable`. Exit code 1. See docs/notes/secrets-doppler-notes.md for the
full session.

Unable to fetch secrets
Doppler Error: Invalid Auth token
